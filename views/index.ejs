<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
   <% for(let post of posts){ %>
        <div class="post">
            <h3 class="user">@<%= post.user %></h3>
            <h4 class="pur"><%= post.purpose %></h4>
             <a href="http://localhost:8080/posts/<%= post.id %>">Show in Details</a>
             <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a><br><br>
             <form method="post" action="posts/<%=post.id%>?_method=DELETE">
                <button>Delete</button>
             </form>
        </div>
         
   <% } %>
   <br>
   <a href="http://localhost:8080/posts/new">Create new post</a>
</body>
</html>